MUTANT25

In this mutant is introduced a reversible actions fault: 

The mutated file is package net.sf.latexdraw.actions.shape.AddShape
The mutated code is the operation undo().

To produce the failure:
1. Draw a shape on canvas
2. Select the shape
3. Perform undo action

Actual result: The action to undo the shape drawing does not work.